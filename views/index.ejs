<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%- include('styles') %>
        <title>Weather App</title>

</head>

<body>

    <form action="/api" method="post">
        <input class="cityinput" type="text" id="city" name="city" placeholder="Enter a city" required>
    </form>

    <div class="weather">
        <p class="city">
            <%= weatherJSON.name + ", " + weatherJSON.sys.country%>
        </p>
        <p class="temp">
            <%= weatherJSON.main.temp + ' °C' %>
        </p>
        <p class="bottom">
            <%= weatherJSON.weather[0].main + " | " + weatherJSON.weather[0].description %>
        </p>

    </div>


</body>

</html